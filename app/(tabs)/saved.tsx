import React from "react";
import { Text, View } from "react-native";

export default function saved() {
  return (
    <View className="flex bg-primary items-center justify-center flex-1">
      <Text className="text-3xl">this is saved</Text>
    </View>
  );
}
